# Progress Tracker

Update this file whenever the current phase, active feature, or implementation state changes.

## Current Phase

- Major scope pivot: added a third tier (Student Union / Institution) above Club. `project-overview.md` and `architecture-context.md` are rewritten for this. Feature specs 01-02 implemented as written; 03-auth.md turned out not to need revision (it already scoped itself as temporary self-serve-org scaffolding — see Open Questions note) and is now implemented; 05-prisma.md is now implemented; specs 04+ from the original roadmap are superseded by a new union-first order below.

## Current Goal

- All union-facing specs through 11 (club application form, union dashboard shell, club application approval queue, event approval queue, funding request queue) are now written and implemented. Continue with the remaining union-oversight queue (overspend-flagging) ahead of Events/Brainstorm — per the decision to prioritize the sellable union-oversight core first.

## Completed

- 00-globals-setup.md — `app/globals.css` now defines the full dark-only token set from `ui-context.md` (`--bg-*`, `--border-*`, `--text-*`, `--accent-*`, `--state-*`), mapped through `@theme inline` to Tailwind utilities (`bg-base`, `bg-surface`, `border-surface-border`, `text-copy-primary`, `text-brand`, `bg-accent-dim`, `text-error`, etc.). No `prefers-color-scheme` block remains; `body` uses `--bg-base`/`--text-primary`. `npm run build` passes. Implemented in `club-hub/club-hub` (the nested copy with git history + installed deps — `club-hub/` outer folder is a stray duplicate from a prior `create-next-app` run, not yet cleaned up).
- 01-design-system.md — `shadcn/ui` installed (`components.json`, style `base-nova`, base color neutral) with all 12 required components (`button`, `card`, `dialog`, `input`, `tabs`, `textarea`, `scroll-area`, `select`, `avatar`, `badge`, `dropdown-menu`, `table`) in `components/ui/`, plus `lucide-react` and `lib/utils.ts` (`cn()` via `clsx` + `tailwind-merge`). shadcn's init wrote its own light-mode `--background`/`--foreground`/etc. tokens and an inactive `.dark` class into `app/globals.css` — remapped those semantic tokens directly onto the existing dark palette (`--background: var(--bg-base)`, etc.) and deleted the unused `.dark` block, since the app is dark-only and nothing ever toggles that class. Also fixed a self-referential `--font-sans: var(--font-sans)` left by init and removed a duplicate plain `body` rule superseded by the `@layer base` one.
  - Found and fixed a real bug via live rendering: our `--color-base` token (for the page-background utility) collided with Tailwind's built-in `text-base` font-size utility name, silently turning `text-base` into `color: var(--bg-base)` (near-black) instead of a font-size — this made `CardTitle` and `DialogTitle` text nearly invisible (near-black-on-black) since both use `text-base` for sizing. Renamed the token to `--color-page` / utility `bg-page`; `ui-context.md` and `code-standards.md` updated to match (was `bg-base`, now `bg-page`). Lesson: custom color-token names must avoid Tailwind's reserved font-size/spacing scale keywords (`base`, `sm`, `lg`, `xl`, `2xl`, etc.).
  - Brand accent changed from the spec's original cyan (`#00c8d4`) to orange (`#ff8a00`) per explicit user request; `ui-context.md` updated (`--accent-primary`, `--accent-primary-dim`, and the descriptive text).
  - Verified with a full component smoke-test page rendered live in dev server + headless Chrome screenshots (not just build success) — caught the above contrast bug this way, which `npm run build` alone did not surface.
- 02-app-chrome.md — `components/app/app-navbar.tsx` (fixed-height, left/center/right sections, static section title, dark `bg-surface` with bottom border, hamburger toggle in the left section on small screens) and `components/app/app-sidebar.tsx` (persistent on desktop via `md:static md:translate-x-0`, slides in as an overlay on mobile via a `translate-x` transform driven by shared state, `bg-surface` + `border-default` right edge, club-switcher placeholder button, Team/Events/More nav with `lucide-react` icons, user menu placeholder with `Avatar`+name) implemented in `club-hub/club-hub`. Mobile open/closed state is shared between navbar and sidebar via a small client context, `components/app/app-shell-context.tsx` (`AppShellProvider`/`useAppShell`) — not spec'd explicitly but necessary since the hamburger toggle (navbar) and the collapsible panel (sidebar) are separate components.
  - Dialog pattern implemented as `components/app/app-dialog.tsx`, a thin wrapper over the existing `components/ui/dialog.tsx` (base-ui-backed, not Radix) supporting `title`/`description`/`footer` props, styled with `rounded-3xl` per the modal radius convention in `ui-context.md`. Only the reusable pattern was built — no real feature dialogs yet, per spec.
  - `app/page.tsx` replaced the 01-design-system smoke-test content with a live chrome preview (sidebar + navbar + example dialog trigger) so the new components render somewhere; the old design-system preview content was not preserved elsewhere since the spec's "check when done" criteria only require the new chrome to compile/lint/behave correctly, not a permanent component gallery. Note: as of 03-auth.md, `/` is no longer reachable (proxy redirects every request away from it), so this preview content is effectively dead — left in place since removing it wasn't in scope for the auth spec.
  - Verified live in dev server with Playwright screenshots (desktop 1440px, mobile 390px closed/open, dialog open) — confirmed persistent sidebar on desktop, slide-in overlay behavior on mobile triggered by the navbar hamburger, and correct title/description/footer rendering in the dialog. `npm run lint` and `npm run build` both pass.
- 03-auth.md — implemented as written (the spec's temporary-scaffolding note already covers the two-Clerk-org-kind concern noted below in Open Questions — see that note for why this wasn't blocked on the Prisma revision). `@clerk/nextjs` + `@clerk/ui` installed (spec said Clerk was "already installed and connected" — it was not; neither package was in `package.json` and no `.env` existed. Installed both and added `.env.local` / `.env.example` with `NEXT_PUBLIC_CLERK_PUBLISHABLE_KEY`, `CLERK_SECRET_KEY`, `NEXT_PUBLIC_CLERK_SIGN_IN_URL=/sign-in`, `NEXT_PUBLIC_CLERK_SIGN_UP_URL=/sign-up` — these are now the canonical env var names since none existed before). `ClerkProvider` wraps `app/layout.tsx` using `@clerk/ui/themes`' `dark` theme, with `variables` overridden to the app's CSS custom properties (`var(--accent-primary)`, `var(--bg-surface)`, etc. — no hardcoded hex in the override). `proxy.ts` at the project root uses `clerkMiddleware` from `@clerk/nextjs/server`: public paths (`/sign-in`, `/sign-up`) pass through, unauthenticated users redirect to `/sign-in`, authenticated users with no `orgId` redirect to `/select-club`, authenticated users with an `orgId` redirect to `/team` (and away from `/select-club` if they land there with one already). Sign-in/sign-up pages (`app/sign-in/[[...sign-in]]`, `app/sign-up/[[...sign-up]]`) share a new `components/app/auth-layout.tsx` two-panel layout (logo/tagline/feature list left, centered Clerk form right, form-only on mobile via `hidden lg:flex`) — no gradients, hero sections, feature cards, or scroll-heavy layout. `/select-club` renders Clerk's `OrganizationList` (`hidePersonal`, redirects to `/team` after create/select). `/team` is a minimal placeholder page reusing the existing `AppSidebar`/`AppNavbar` chrome. Sidebar's club-switcher and user-menu placeholders (`components/app/app-sidebar.tsx`) replaced with real `OrganizationSwitcher` and `UserButton` (`hidePersonal`, `showName`), restyled via each component's `appearance.elements` (`organizationSwitcherTrigger`, `userButtonTrigger`, etc.) rather than custom-built — Clerk's default menus/flows are untouched.
  - Used `clerkMiddleware` (not `createRouteMatcher`) for path checks — `createRouteMatcher` is deprecated in the installed `@clerk/nextjs` version (its own doc comment recommends resource-based per-route auth checks instead, since middleware path-matching can drift from actual Next.js routing). A plain prefix check against the sign-in/sign-up env vars is used instead. Per the Next.js 16 proxy docs, this proxy-level check is a redirect-UX convenience only, not the enforcement boundary — real mutation/query handlers must still verify auth themselves (already an explicit invariant in `architecture-context.md`).
  - Verified live: `npm run build` and `npm run lint` both pass. Confirmed via `curl` that unauthenticated requests to `/`, `/select-club`, and `/team` all 307-redirect to `/sign-in`, while `/sign-in` itself returns 200. Confirmed via Playwright screenshots (1440px and 390px) that sign-in/sign-up render the two-panel dark layout correctly with the orange accent and no hardcoded colors. Left `.env.local`'s Clerk keys blank — Clerk's Next.js SDK auto-provisioned a working **keyless dev-mode instance** on first run (logged a claim URL + shows a "Missing environment keys" dev banner), which is how the live screenshots were taken; whoever picks this up should claim those keys or swap in a real Clerk app's keys before shipping past local dev.

- 05-prisma.md — implemented in `club-hub/club-hub`. Prisma schema converted from a single `prisma/schema.prisma` to Prisma 7's native multi-file layout: `prisma/schema.prisma` now holds only `generator`/`datasource`, and all models live in `prisma/models/*.prisma` (`institution.prisma`, `form-schema.prisma`, `club.prisma`, `approvals.prisma`, `event.prisma`, `budget.prisma`, `brainstorm.prisma`) per the spec's file layout. `prisma.config.ts`'s `schema` path changed from the single file to the `prisma` directory so Prisma merges all `.prisma` files in it. All models from the spec added as written: `Institution`, `UnionStaffOrgLink`, `FormSchema` (+ `FormType` enum), `Club`, `ClubApplication` (+ `ApprovalStatus` enum shared across Club/ClubApplication/EventApprovalRequest/FundingRequest), `EventApprovalRequest`, `FundingRequest`, `Event` (+ `EventStatus` enum), `EventTask`, `BudgetCategory`, `BudgetEntry` (+ `BudgetEntryType` enum), `BrainstormIdea` — relations, cascade/set-null FK behavior, and indexes all match spec. No local Club membership/role table — Clerk orgs remain the only source of truth for that, as required.
  - `lib/prisma.ts` rewritten to branch on `DATABASE_URL`: `prisma+postgres://` prefix uses Prisma 7's native `accelerateUrl` client option (no separate `@prisma/extension-accelerate` package or `.$extends()` needed — Prisma 7 supports Accelerate directly via `new PrismaClient({ accelerateUrl })`), otherwise falls back to the existing `@prisma/adapter-pg` direct-connection path. Still cached on `global` in development. The project's actual `DATABASE_URL` is a direct `postgres://` URL, so the adapter-pg branch is what's live; the Accelerate branch is untested against a real `prisma+postgres://` URL since none is configured, but compiles and type-checks correctly against the generated client's `PrismaClientOptionsWithAccelerateUrl` variant.
  - `lib/org-scope.ts` (new) — `getActiveOrgId()` reads `orgId` from Clerk's `auth()` and throws if absent; `getActiveClub()` additionally resolves the `Club` by `clerkOrgId` and throws if none exists. `lib/institution-scope.ts` (new) — `getActiveInstitution()` reads `orgId` from `auth()`, looks it up in `UnionStaffOrgLink`, and throws if the org isn't a recognized Union Staff org. The two are intentionally not shared/parameterized into one helper despite the structural similarity, since the spec calls out that a union route resolving institution scope and a club route resolving org scope must never be interchanged.
  - Migration: the existing `prisma/migrations/` only contained the `create-next-app`-style starter demo (`Club { name }` / `Member` models, 2 seeded clubs + 3 seeded members in the dev Postgres at db.prisma.io) — structurally incompatible with the new schema (new required columns, changed `Club` meaning entirely). Old migration deleted and dev database reset (`prisma migrate reset --force`, run only after explicit user consent per Prisma's AI-agent safety gate, since this destroys all rows) since it held only disposable starter/seed data, not real user data. Fresh single migration `20260721194551_init_full_schema` created and applied covering all 12 tables. `prisma/seed.ts` (which upserted the old Club/Member starter rows) emptied to a no-op stub since its models no longer exist — no replacement seed data was in scope for this spec.
  - Verified: `npx prisma validate` and `npm run build` both pass (build exercises `prisma generate` via the `prebuild` script, then full Next.js typecheck + static generation).
  - Nothing was skipped — the pre-existing `prisma/schema.prisma`, `lib/prisma.ts`, and migration were all the unrelated starter-template scaffold, not a prior implementation of this spec, so everything here was newly created rather than left alone.

- 06-form-schema-builder.md — implemented in `club-hub/club-hub` as a standalone route, `app/union/forms/page.tsx`, with no union dashboard chrome (per spec, that's a later spec) — just a centered max-width column. Server component: calls `getActiveInstitution()` (from 05-prisma.md's `lib/institution-scope.ts`) directly and catches its thrown error to render a plain centered "You don't have access to this page" state, since that helper throws rather than returning null. Real enforcement is two-layered — `proxy.ts`'s existing Clerk middleware already redirects unauthenticated users before this page's own check ever runs (matcher covers all non-static routes including `/api/*`), and the page/route-level `getActiveInstitution()` call is the actual institution-scoping boundary per `architecture-context.md`'s invariant that route handlers must verify org/institution ownership themselves.
  - Seeding + shared field types live in new `lib/form-schema-defaults.ts`: `FormField`/`FieldType` types (`TEXT`/`TEXTAREA`/`NUMBER`/`BOOLEAN`/`SELECT`/`DATE`/`FILE`, matching `architecture-context.md`'s "Configurable Forms" section) and per-form-type default field arrays matching the spec's starter fields exactly (club name/description/category/proposed admin name+email/expected member count; event name/date/location/description/presentation boolean/speaker names optional/physical-activities boolean; funding amount/reason). `getOrSeedFormSchema(institution, formType)` does a `findUnique` on the `institutionId_formType` compound unique index and creates-with-defaults on miss — used by both the GET route and the server page (so the page always has a schema to render, seeding on first load exactly once thanks to the unique constraint).
  - API: `app/api/union/forms/[formType]/route.ts`, `GET` and `PUT`, using Next 16's `RouteContext<'/api/union/forms/[formType]'>` helper type (per this project's vendored Next docs at `node_modules/next/dist/docs`) rather than manually typing `params: Promise<{...}>`. Both handlers validate `formType` against the three enum values before touching the database, and both resolve the institution via `getActiveInstitution()` wrapped in a try/catch that returns a 403 JSON response instead of letting the throw bubble into a 500 — the thin route-handler convention from `code-standards.md` pushed the actual validation logic (`parseFields`, checking every field's `type`/`required`/`options`/`helpText` shape) into a private helper in the same file rather than a shared lib module, since it's only ever called from this one route. `PUT` does a full-list `upsert` (not a diff/patch) keyed on the same compound unique index, matching the spec's "replaces the full field list" contract.
  - Builder UI: `components/union/form-schema-builder.tsx` (`Tabs` per form type, each mounting an independent `FormTypeEditor` with its own field-array state and its own save state, so switching tabs doesn't lose unsaved edits in another tab) + `components/union/field-editor-dialog.tsx` (the add/edit `Dialog`, prefilled from the clicked field row when editing, empty defaults when adding). Reordering uses up/down icon buttons (`lucide-react` `ArrowUp`/`ArrowDown`), not drag-and-drop — the spec explicitly allows either, and this project has no existing drag-reorder pattern to reuse, so buttons were the lower-complexity choice. Delete has no separate confirm dialog per spec ("no need for a separate confirmation dialog for this unit") — it's a single destructive-styled icon button. `fieldKey` is auto-derived from the label via a `slugify` helper on add (deduplicated against existing keys by appending `_2`, `_3`, etc. if needed) and preserved as-is on edit, since `architecture-context.md` requires `fieldKey` stability for `schemaSnapshot` correctness on past submissions — nothing in this unit writes submissions yet, but the key scheme was chosen with that invariant in mind for the next spec.
  - Two shadcn components the project didn't have yet were hand-written rather than pulled via the `shadcn` CLI (which hung indefinitely in this environment, unrelated to the component content — see below): `components/ui/switch.tsx` (thin wrapper over `@base-ui/react/switch`, mirroring the existing `select.tsx`/`tabs.tsx` base-ui-primitive pattern already used in this repo) and `components/ui/label.tsx` (plain `<label>`, not base-ui's `Field.Label`, since that primitive requires being nested in a `Field.Root` context that this form doesn't otherwise use).
  - Verified: `npm run build` passes (full TypeScript check + static generation, `/union/forms` and `/api/union/forms/[formType]` both appear in the route output as expected — dynamic/server-rendered, not static). Confirmed via `curl` that both `/union/forms` and `/api/union/forms/CLUB_APPLICATION` 307-redirect to `/sign-in` when unauthenticated, same as every other protected route. Did **not** get to manually exercise the add/edit/reorder/delete/save-then-reload flow live in a browser — that requires an authenticated session in a real Union Staff Clerk org, which isn't available in this headless environment (no such org/seed data exists yet either). This is a gap relative to this project's usual practice of live-rendering UI changes before calling them done (per `code-standards.md`'s Next.js section and prior specs' verification notes) — whoever picks this up next should manually click through the three tabs' add/edit/reorder/delete/save-reload cycle before treating this as fully done, not just build-clean.
  - Environment note, not a code issue: `npm run build`, `npm run lint`, and even plain `npx prisma generate` intermittently hung or threw `ETIMEDOUT: connection timed out, read` from Node's `fs.readFileSync` in this sandbox, unrelated to any change made here (reproduced on totally unrelated `node_modules` files). Also found and stopped a stray `next dev` process left running from a previous session that was holding `.next/lock` and blocking `next build` from ever acquiring it — killed it (after checking with the user first) before the build could complete. `npm run lint` never completed cleanly after several retries (same stalling pattern); relying on `npm run build`'s full TypeScript pass as the correctness check instead, since that's the spec's actual "Check When Done" requirement, not lint.

- 07-club-application-flow.md — implemented in `club-hub/club-hub`. `/apply/[institutionSlug]` (`app/apply/[institutionSlug]/page.tsx`) is a server component: looks up the `Institution` by slug via a new `getInstitutionBySlug` helper in `lib/institution-scope.ts` (plain `findUnique`, no auth/org involved — distinct from that file's existing `getActiveInstitution`, which is org-scoped), renders "This institution isn't set up yet." if missing, otherwise calls 06's `getOrSeedFormSchema(institution, "CLUB_APPLICATION")` and passes the fields into a client `<ApplyForm>`. Submission posts to `POST /api/institutions/[institutionSlug]/applications`, then client-side redirects to `/apply/[institutionSlug]/confirmation` (a second small server page, also slug-driven so it survives a refresh) rather than swapping in-place state, matching the spec's literal "redirect to a confirmation screen."
  - `components/forms/dynamic-form.tsx` — the generic field-renderer the spec asked for, reusable by later event-approval/funding-request work. Note on provenance: the spec says to "extract" this from 06's builder, but 06's builder only ever rendered *schema-editing* UI (type dropdown, options textarea, required switch) — there was no existing *answer-collecting* renderer to extract, so this was built fresh against the same `FormField[]` type instead. Field-type mapping: TEXT→`Input`, TEXTAREA→`Textarea`, NUMBER→`Input type=number`, DATE→`Input type=date`, SELECT→shadcn `Select`, BOOLEAN→a Yes/No two-button toggle (no radio-group primitive exists in this repo's `components/ui/`, and spec allowed either toggle or radio pair), FILE→disabled `Input` with "File uploads coming soon" placeholder, per scope limit.
  - `lib/validate-form-answers.ts` (new, isomorphic — no server-only imports) — single `validateFormAnswers(fields, answers)` used both client-side (in `ApplyForm`, pre-submit) and server-side (in the API route), so required/type validation can't drift between the two per the spec's explicit "both client and server" requirement.
  - Reserved-field mismatch found and fixed: 06's seeded `CLUB_APPLICATION` defaults used `fieldKey: "proposed_admin_email"` (snake_case), but this spec requires the reserved key to be exactly `"proposedAdminEmail"` (camelCase) so the guard has something stable to check against. Renamed the default's `fieldKey` in `lib/form-schema-defaults.ts` and added an exported `PROPOSED_ADMIN_EMAIL_FIELD_KEY` constant so the seed default and the submission-time guard reference the same literal instead of two independently-typed strings. This only affects newly-seeded schemas (`getOrSeedFormSchema` seeds once per institution on first access) — no real `FormSchema` rows existed yet to migrate, per 06's own note that no Union Staff org/institution seed data exists in this environment.
  - Guard behavior (`app/api/institutions/[institutionSlug]/applications/route.ts`): looks up the reserved field by key in the *current* schema's fields; if present, pulls the submitted answer for it as `proposedAdminEmail`; if a union has since deleted that field from their schema, falls back to `currentUser()`'s (`@clerk/nextjs/server`) primary email and logs a `console.warn` — submission is never blocked on this, per spec. Verified both paths with a standalone `tsx` script exercising `validateFormAnswers` and the reserved-field-removed case directly (real DB/Clerk-session testing wasn't available — see below).
  - `proxy.ts` changed: previously *every* authenticated route redirected a user with no `orgId` to `/select-club`, which would have made `/apply/[institutionSlug]` unreachable for the exact user this spec targets (a student with no club org yet). Added a `NO_ORG_REQUIRED_PATHS` prefix list (`/select-club`, `/apply`, `/api/institutions`) that skips the org check while still requiring sign-in. Also added `redirect_url` query-param preservation on the unauthenticated→`/sign-in` redirect (Clerk's `SignIn`/`clerkMiddleware` stack reads this param to send the user back to their original destination after signing in), which proxy.ts hadn't done before for any route — this is a small behavior improvement beyond this spec's page but was necessary to satisfy "redirect to `/sign-in` and back here after signing in."
  - Verified: `npm run build` passes (new routes `/api/institutions/[institutionSlug]/applications`, `/apply/[institutionSlug]`, `/apply/[institutionSlug]/confirmation` all appear correctly as dynamic/server-rendered). Confirmed via `curl` that unauthenticated requests to both the page and the POST endpoint 307-redirect to `/sign-in?redirect_url=...` with the original path preserved. Live-rendered the actual field renderer via a temporary (not committed — created, screenshotted, then fully deleted, including reverting a temporary `proxy.ts` allowlist entry used only to reach it) debug route with Playwright: confirmed every field type renders correctly (required-field validation messages, Select dropdown opening with options, the Yes/No boolean toggle highlighting on click, native date picker, disabled file placeholder), and confirmed the "institution not set up yet" and confirmation-screen states render as expected. Did **not** exercise the true end-to-end golden path (real signed-in Clerk user with no org, hitting a real seeded `Institution`, submitting, and landing in the DB as a `ClubApplication` row) — this environment has no Union Staff org or `Institution` row seeded yet (same gap noted in 06-form-schema-builder.md), so that full loop is still unverified live. Whoever picks up 09-club-application-queue.md should do this click-through once real institution/org seed data exists, to also confirm the queue can actually read back what this spec writes.

- 08-union-dashboard-shell.md — implemented. `lib/get-org-mode.ts` (new) is the single source of truth for chrome-level org mode: `getOrgMode()` resolves the active Clerk org's mode by calling the *existing* `getActiveInstitution()` (`lib/institution-scope.ts`) and `getActiveClub()` (`lib/org-scope.ts`) concurrently, each wrapped in `.catch(() => null)`, rather than re-querying `UnionStaffOrgLink`/`Club` directly — deliberately reusing the two lookups the spec called out instead of a third independent implementation of the same membership check. Returns a discriminated union: `{ mode: "union", institution }` / `{ mode: "club", club }` / `{ mode: "none" }`. `requireOrgMode(required)` (also in that file, overloaded so `requireOrgMode("union")` types as `Promise<Institution>` and `requireOrgMode("club")` types as `Promise<Club>`) is the page-level enforcement primitive: redirects to `/org-not-configured` on `"none"`, cross-redirects (`/team` ↔ `/union/applications`) on a mode mismatch, and otherwise returns the already-resolved `Institution`/`Club` record — so callers never issue a second redundant lookup for the same data.
  - `components/app/app-shell.tsx` (new) — a small presentational server component (`{ mode, title, children }`) factoring out the sidebar+navbar+main composition that `app/team/page.tsx` previously inlined, now shared by `/team` and all four union routes. It does no auth/data work itself (that's `requireOrgMode`, called by each page before rendering `AppShell`) — kept this way so the access-control decision and the chrome-rendering stay separably testable, per `code-standards.md`'s "don't mix unrelated concerns" guidance.
  - `components/app/app-sidebar.tsx` extended (not rebuilt, per spec): now takes a `mode` prop, branches `NAV_ITEMS` into `CLUB_NAV_ITEMS` (Team/Events/More, unchanged) vs `UNION_NAV_ITEMS` (Applications `ClipboardList`, Events `CalendarDays`, Funding `Landmark`, Forms `FileText`, all `lucide-react`), and renders a small `Badge` ("Union"/"Club") above the `OrganizationSwitcher`. Nav items gained an optional `href`: items with a real destination now render as a `next/link` `Link` (Team → `/team`, and all four union items → their real routes); items with no built route yet (club Events/More) still render as inert, non-navigating `<button>`s exactly as before — those routes remain out of scope per the original 02-app-chrome.md placeholder and this spec's scope limits.
  - Three new placeholder routes, matching the Team page's existing Card pattern exactly (title + one description line, wrapped in `AppShell`): `app/union/applications/page.tsx` ("Club applications awaiting review will live here."), `app/union/events/page.tsx` ("Event approval requests will live here."), `app/union/funding/page.tsx` ("Funding requests will live here.") — each calls `requireOrgMode("union")` before rendering.
  - `app/union/forms/page.tsx` rewired per the progress-tracker's own forward-pointer from 06-form-schema-builder.md ("`/union/forms` is currently a standalone route with no chrome... will need wiring into real nav here") — replaced its ad hoc `getActiveInstitution()` try/catch + chromeless centered-column layout with `requireOrgMode("union")` + `AppShell`, so it now renders inside the same sidebar/navbar as every other union route instead of being reachable only by typing the URL. Behavior (schema seeding, tabs, builder UI) is unchanged.
  - `app/team/page.tsx` updated to call `requireOrgMode("club")` before rendering (previously had no mode/access check at all beyond the proxy's generic "has an org" gate) and now composes via `AppShell` instead of inlining the sidebar/navbar JSX directly.
  - New `app/org-not-configured/page.tsx` — the "none" mode's landing page (a plain centered Card, no sidebar since mode is unknown at that point), reached via `requireOrgMode`'s redirect rather than rendered inline by a component that would otherwise need to conditionally skip the chrome. `app/page.tsx`'s pre-existing (already-dead, per 02-app-chrome.md's note that `/` is unreachable post-auth) preview content needed a one-line `mode="club"` fix to keep compiling against `AppSidebar`'s new required prop — not otherwise touched.
  - Access enforcement is intentionally page-level, not `proxy.ts`-level: confirmed via the vendored Next.js 16 docs (`node_modules/next/dist/docs/01-app/01-getting-started/16-proxy.md`) that Proxy (the renamed Middleware) is explicitly documented as unsuitable for "slow data fetching" or as "a full session management or authorization solution" — consistent with how 06/07 already did their institution-scoping in the page/route itself, not in `proxy.ts`. No `proxy.ts` changes were needed for this spec (all five new/changed routes already require an org via the existing `NO_ORG_REQUIRED_PATHS` gate, and mode-correctness is `requireOrgMode`'s job).
  - Verified: `npm run build` passes (all five routes appear correctly as dynamic/server-rendered: `/team`, `/union/applications`, `/union/events`, `/union/funding`, `/union/forms`, plus `/org-not-configured` as static). Confirmed via `curl` that all six 307-redirect to `/sign-in` when unauthenticated, consistent with every other protected route. `npm run lint` completed this time (previous specs noted it hanging in this sandbox) and surfaces exactly one pre-existing error in `components/union/field-editor-dialog.tsx` (a `setState`-in-effect rule violation from 06-form-schema-builder.md) — confirmed via `git diff --stat` that this file wasn't touched here, so it's a carried-over gap, not a regression; whoever picks up the form-schema-builder work next should fix it. Live-verified the actual mode-branching visually: no Union Staff org/Institution/Club/Clerk-org seed data exists in this environment (same recurring gap noted in 06 and 07), so a true authenticated end-to-end click-through (switching a real Clerk `OrganizationSwitcher` between a real Club org and a real Union Staff org and watching the sidebar update) still isn't possible here. Instead, used the same technique as 07's verification: a temporary, uncommitted `app/debug-shell/page.tsx` (rendering `AppShell` directly with `mode` driven by a query param, bypassing `requireOrgMode` entirely since `AppShell` itself does no auth work) plus a temporary `proxy.ts` public-path entry, screenshotted both modes at 1440px and the mobile hamburger open/closed states at 390px with Playwright, then fully deleted the debug route and reverted `proxy.ts` (confirmed clean via `git diff proxy.ts` afterward). Screenshots confirmed: correct nav items + icons + badge text per mode ("Club" badge with Team/Events/More; "Union" badge with Applications/Events/Funding/Forms), correct navbar title per page, and no regression in the mobile slide-in/hamburger behavior. Did **not** verify the real `OrganizationSwitcher`'s live-switching behavior (component itself untouched, per spec, and Clerk's switcher needs a real multi-org session to exercise) — whoever seeds real Union Staff + Club test orgs next should do that one live click-through.

- 09-club-application-queue.md — implemented. `app/union/applications/page.tsx` replaced its 08-shell placeholder: a server component that resolves the institution via `requireOrgMode("union")` and queries `prisma.clubApplication.findMany({ where: { institutionId, status: "PENDING" }, orderBy: { createdAt: "desc" } })` directly (same direct-prisma-in-page pattern as 06/08, not routed through the new GET API — see below), passing plain-object list items into a client `ApplicationsQueue`.
  - `components/forms/answer-summary.tsx` (new) — the read-only field-and-answer renderer the spec asked for, deliberately separate from `dynamic-form.tsx` (07's editable renderer). Pure presentational, no `"use client"` needed. Formats `BOOLEAN` as Yes/No, `NUMBER` with `toLocaleString()`, blank/missing values as "—", everything else via `String(value)`.
  - Reserved a second field key alongside 07's `PROPOSED_ADMIN_EMAIL_FIELD_KEY`: `CLUB_NAME_FIELD_KEY = "club_name"`, exported from `lib/form-schema-defaults.ts` and used to pull the list's club-name column out of `answers` (spec: "it's always present since it's a required seeded field"). Unlike the admin-email key, there's no submission-time guard forcing its presence — if a union deletes this field from their schema, the queue just falls back to displaying "Untitled application" rather than blocking anything, since nothing downstream depends on this value being correct (Club creation reads it too, with the same fallback, described below).
  - `components/union/applications-queue.tsx` (client) — table (first real use of `components/ui/table.tsx` in this app) of pending applications with a "Review" button per row; owns which application's dialog is open. `components/union/application-review-dialog.tsx` (client) — a single dialog with three internal views (`detail` / `approve` / `reject`) switched by local state rather than three separate dialogs, matching the spec's "Show this as a small form within the approve confirmation (don't make it a separate step)" instruction. Detail view renders `AnswerSummary` against the application's own `schemaSnapshot` (never the live `FormSchema` — matches architecture-context.md invariant 6) plus the resolved `proposedAdminEmail` as its own row underneath (intentionally can duplicate a schema-rendered answer of the same value — the spec calls out both as separate required pieces of the view since they can diverge, e.g. if a union later deletes the admin-email field from their schema). Approve view is a numeric baseline-budget input; reject view is a destructive-styled confirmation. Used raw `Dialog` primitives directly rather than the shared `AppDialog` wrapper (`components/app/app-dialog.tsx`) — same deviation `field-editor-dialog.tsx` already made in 06, since `AppDialog` is locked to `sm:max-w-sm` and this content needs more room; wrapped the detail view's answer list in `ScrollArea` (first use of that installed-but-previously-unused shadcn component) since a long schema could otherwise overflow the dialog.
  - Deliberately did **not** add a `useEffect` to reset the dialog's internal view/state on open, unlike `field-editor-dialog.tsx`'s (pre-existing, still-unfixed) pattern that trips the `react-hooks/set-state-in-effect` lint rule — `ApplicationsQueue` only mounts `ApplicationReviewDialog` at all when an application is selected (`{selected && <ApplicationReviewDialog ... />}`), so the dialog fully unmounts/remounts on every open/close cycle and `useState`'s initializers already do the reset for free. `npm run lint` confirms this introduced zero new errors — the one pre-existing error in `field-editor-dialog.tsx` (noted in 08's entry) is untouched, carried over.
  - API: `app/api/union/applications/route.ts` (`GET`, `?status=` query param, defaults to `PENDING`, validated against the `ApprovalStatus` enum) built per spec's explicit API list, but note the union-applications *page* does not call it — it queries prisma directly instead, consistent with how 06's and 08's pages already work. The GET route exists as the spec-mandated API surface (and for whatever consumes it later) rather than as the page's actual data source.
  - `app/api/union/applications/[applicationId]/approve/route.ts` — resolves the institution via `getActiveInstitution()` (403 on failure), loads the `ClubApplication` by ID and rejects (404) unless `application.institutionId === institution.id` — the explicit "never trust the `applicationId` alone" check from the spec — then 409s if it's not still `PENDING`. Validates `baselineBudgetAmount` as a non-negative finite number. Sequence: `clerkClient().organizations.createOrganization({ name })` (name resolved via the same `CLUB_NAME_FIELD_KEY` fallback as the list) → `createOrganizationInvitation({ organizationId, emailAddress: application.proposedAdminEmail, role: "org:admin" })` → `prisma.$transaction` creating the `Club` row (`institutionId`, `clerkOrgId`, `approvalStatus: "APPROVED"`, the entered baseline budget) and updating the `ClubApplication` (`status: "APPROVED"`, `resultingClubId`) together. Deliberately did **not** pass `createdBy` on `createOrganization` — Clerk would auto-admit that user as an org admin, but the submitter (`application.proposedAdminUserId`, used only for the `ClubApplication` record) and the proposed admin (`application.proposedAdminEmail`) are not guaranteed to be the same person (a student can submit on someone else's behalf), so admin membership is granted solely through the explicit invitation, matching the spec's two-separate-steps description. Any failure after org creation (invitation failure or transaction failure) is caught and triggers a best-effort `client.organizations.deleteOrganization(org.id)`, logged either way — matches architecture-context.md invariant 4 and the spec's explicit cleanup instruction. Role string `"org:admin"` matches the existing convention in `scripts/setup-union-staff-test-org.ts` (the only other place in this codebase driving Clerk's Organizations backend API so far).
  - `app/api/union/applications/[applicationId]/reject/route.ts` — same ownership/status checks, then a single `status: "REJECTED"` update. No Clerk calls, no Club record, matching spec's "status change alone is the outcome."
  - Verified: `npm run build` and `npm run lint` both pass (four new routes appear correctly as dynamic/server-rendered: `/union/applications`, `/api/union/applications`, `/api/union/applications/[applicationId]/approve`, `/api/union/applications/[applicationId]/reject`; the one lint error present is the pre-existing `field-editor-dialog.tsx` one from 06, confirmed untouched here). Confirmed via `curl` that all four 307-redirect to `/sign-in` when unauthenticated. Live-verified the actual approve/reject business logic — not just types — with a temporary, uncommitted script (`scripts/_tmp-verify-application-queue.ts`, deleted after use) run against the real dev Postgres and the real Clerk backend API (this environment now has `CLERK_SECRET_KEY` configured with a live Clerk dev instance, plus a real seeded `test-university` `Institution`/`UnionStaffOrgLink` from `scripts/setup-union-staff-test-org.ts` — the golden-path seed-data gap called out repeatedly in 06/07/08 is now partially closed, at least for scripted/backend verification). The script created a real pending `ClubApplication`, ran the exact approve sequence (create org → invite → transaction), and confirmed the `Club` row and `ClubApplication.status/resultingClubId` end up correct, then cleaned up (deleted the test `Club` and Clerk org). It also forced the transaction step to fail (bad `institutionId`, FK violation) after the Clerk org and invitation had already been created, confirmed the route's cleanup path deletes that org (queried Clerk afterward to confirm it's actually gone, not just that `deleteOrganization` didn't throw), and confirmed the reject flow only flips `status` with zero other side effects. Also rendered the real `ApplicationsQueue`/`ApplicationReviewDialog` components live (temporary uncommitted debug route + temporary `proxy.ts` public-path entry, same technique as 07/08, fully reverted after) and screenshotted all four states (list, detail, approve, reject) with Playwright at 1440px — table layout, scrollable answer summary, and both confirmation sub-views all render as intended. Did **not** complete a true end-to-end **browser** click-through as a signed-in Union Staff user (submit via `/apply/test-university`, then review/approve/reject it through the real UI) — this environment has a real Clerk instance now but no way to complete an interactive sign-in (no password/OTP access), so that last gap from 06/07/08 persists; whoever has an actual authenticated session against the `test-university` org should do this once.
  - Found (did not fix, out of scope for this spec): the live `test-university` institution's seeded `CLUB_APPLICATION` `FormSchema` still has the *old* snake_case `proposed_admin_email` field key from before 07-club-application-flow.md's camelCase fix (`getOrSeedFormSchema` only seeds once, and this row was apparently seeded before that fix landed). This doesn't break anything here — `ClubApplication.proposedAdminEmail` is always read from its own dedicated column, never re-derived from `answers`/`schemaSnapshot` — but it does mean any *new* application submitted against this specific seeded institution right now would hit 07's documented fallback (submitter's own email, with a `console.warn`) instead of using an explicitly-answered proposed admin email. Whoever seeds a fresh test institution next, or touches that flow again, should just re-seed (delete the stale `FormSchema` row and let `getOrSeedFormSchema` recreate it) rather than patch the old data in place.

- 10-event-approval-queue.md — implemented. Club-side: `app/events/request-approval/page.tsx` is a standalone route with no dashboard chrome (same reasoning as 06's original standalone `/union/forms` — this isn't wired into any nav yet since there's no real Events board to link from). It calls `getActiveClub()` (`lib/org-scope.ts`) directly and renders a plain "You don't have access to this page" state on the thrown error — any club member can reach it, not just admins, since `getActiveClub()` never checks Clerk org role. Resolves the institution via `club.institutionId`, then reuses `getOrSeedFormSchema(institution, "EVENT_APPROVAL")` (06) and renders it through the existing `DynamicForm` (07). `components/events/request-approval-form.tsx` and `app/events/request-approval/confirmation/page.tsx` mirror 07's `ApplyForm`/confirmation pattern exactly, minus the slug param (this route is auth/org-scoped, not slug-driven). `POST /api/events/request-approval` follows 07's applications-route shape (parse → `getOrSeedFormSchema` → `validateFormAnswers` → pick non-FILE answers → create), creating an `EventApprovalRequest` (`clubId`, `answers`, `schemaSnapshot`, `status: "PENDING"`) — no new club-membership check beyond `getActiveClub()` since there's no admin-only gate on this one.
  - `lib/form-schema-defaults.ts`: added `EVENT_NAME_FIELD_KEY = "eventName"` and renamed `EVENT_APPROVAL_DEFAULTS`'s first field from the old `event_name` (snake_case) to reference the constant — same rename 07 did to `CLUB_APPLICATION`'s admin-email field, and for the same reason (a stable camelCase key for a value downstream code depends on). Unlike `PROPOSED_ADMIN_EMAIL_FIELD_KEY`, there's no sensible identity to fall back to if a union deletes this field, so the approve route falls back to a placeholder title ("Untitled event") instead — same non-guarded pattern as `CLUB_NAME_FIELD_KEY`. `date`/`location`/`description` were deliberately *not* given reserved constants (spec only asked for one) — the approve route reads them by their literal seeded-default keys and just omits them from the created `Event` if a union has renamed or removed those fields.
  - Originally shipped with a workaround for a data-model gap (`Club` had no `name` column) that batch-fetched Clerk org names via `getOrganizationList` in `app/union/events/page.tsx` — **superseded by the follow-up fix below**, which added a real `Club.name` column instead. See that entry for the current state; this note is kept only as history for why the page briefly called Clerk at all.
  - **Follow-up fix (same day)**: added `name String` (required) to `Club` in `prisma/models/club.prisma` — closes the gap flagged above and generalizes past event approval to the upcoming funding-request/overspend queues, all of which need "which club is this" without re-deriving it from Clerk each time. Two migrations, not one, since the table already had a real row (`psa`, created via 09's live approve-flow verification) and Postgres won't let you add a `NOT NULL` column with no default against existing data: `20260722041538_add_club_name_nullable` (`name TEXT`, nullable) → one-time backfill script (uncommitted, deleted after use — queried Clerk's `getOrganization` once per existing `Club.clerkOrgId` and wrote the result back, confirmed via a live DB read afterward that `psa` backfilled correctly) → `20260722041627_make_club_name_required` (`ALTER COLUMN name SET NOT NULL`, which only succeeds because every row was already populated). `app/api/union/applications/[applicationId]/approve/route.ts` now passes the same `clubName` local (already computed there via `CLUB_NAME_FIELD_KEY`, previously used only to name the Clerk org) into `tx.club.create`'s `data.name` — one value, two writes, not a second independently-derived source of truth. Updated `CLUB_NAME_FIELD_KEY`'s doc comment in `lib/form-schema-defaults.ts` to reflect both call sites. `app/union/events/page.tsx` now reads `request.club.name` directly from the existing Prisma `include: { club: true }` join — the `clerkClient`/`getOrganizationList` call and its helper function are gone entirely, no Clerk API call on this page anymore.
  - This session had live Postgres + Clerk backend API access (unlike the original 10-event-approval-queue.md session, which had neither) — actually exercised the whole chain for real: queried the live DB directly (found exactly one pre-existing `Club` row, `psa`), called Clerk's `getOrganization` directly to confirm its name really is `"psa"` before writing the backfill script (not assumed), ran the two migrations against the real dev database, ran the backfill script, and re-queried the DB afterward to confirm `Club.name = "psa"` stuck. `npm run build` and `npm run lint` both pass (build output confirms all routes still compile; the one pre-existing `field-editor-dialog.tsx` lint error from 06 is untouched). This closes the "unverified against live data" gap the original 10 entry called out for itself.
  - Union-side: `app/union/events/page.tsx` replaces the 08-shell placeholder, following 09's page pattern exactly — `requireOrgMode("union")` then a direct `prisma.eventApprovalRequest.findMany({ where: { status: "PENDING", club: { institutionId } }, include: { club: true } })`, i.e. filtering through the `club` relation rather than a direct `institutionId` column on `EventApprovalRequest` (that model only has `clubId`, per 05-prisma.md's schema — this is the "aggregating across multiple clubs" join the spec calls out as new for this queue). `components/union/events-queue.tsx` / `components/union/event-review-dialog.tsx` mirror 09's `ApplicationsQueue`/`ApplicationReviewDialog` structure (table + `Review` button, three-view dialog switched by local state, dialog only mounted while `selected` is truthy so no `useEffect` state-reset is needed — same reasoning 09 documented for its own dialog). The one deliberate difference: the approve view has no form input (per spec, event approval "doesn't touch money at all," unlike 09's baseline-budget step) — it's just a confirmation view with explanatory text, kept as its own dialog view (not folded into `detail`) for symmetry with the reject confirmation and to guard against a misclick creating a real `Event`.
  - `app/api/union/events/[requestId]/approve/route.ts` — resolves institution via `getActiveInstitution()` (403 on failure), loads the `EventApprovalRequest` with its `club` included, 404s unless `club.institutionId === institution.id` (the same "never trust the ID alone" ownership check 09 used), 409s if not `PENDING`. `$transaction` creates the `Event` (`clubId`, `title` from `EVENT_NAME_FIELD_KEY`, `description`/`location`/`dateTime` from answers where present — `dateTime` parsed from the seeded `date` field's string value via `new Date(...)`, left `null` if missing or unparseable, `status: "CONFIRMED"`) and updates the `EventApprovalRequest` (`status: "APPROVED"`, `resultingEventId`) together. `app/api/union/events/[requestId]/reject/route.ts` — same ownership/status checks, single `status: "REJECTED"` update, no `Event` created, matching spec's "status change alone is the outcome."
  - Per the spec's explicit instruction, flagging (not solving) the integration point for whoever builds 14-events-board.md: this flow always creates a brand-new `Event` on approval. Once clubs can create `IDEA`/`PLANNING` events themselves before requesting approval, approving a request for an event that already exists as a club-created draft will create a *second*, duplicate `Event` rather than confirming the existing one — the submission form will need to let a club link an existing `Event` instead of always implying a new one, or the approve route will need a "does a matching Event already exist" reconciliation step. Nothing here attempts either.
  - Verified: `npm run build` passes — all six new/changed routes appear correctly as dynamic/server-rendered (`/events/request-approval`, `/api/events/request-approval`, `/union/events`, `/api/union/events`, `/api/union/events/[requestId]/approve`, `/api/union/events/[requestId]/reject`) alongside `/events/request-approval/confirmation` as static. `npm run lint` passes with zero new errors (the one pre-existing `field-editor-dialog.tsx` error from 06 is untouched, confirmed via `git diff --stat`). This session's sandbox had no live Postgres or Clerk backend API access at all (unlike 09's session, which had a real dev DB and a live Clerk instance with a seeded `test-university` institution) — a connection check against `DATABASE_URL` returned `ECONNREFUSED` immediately, so none of the deeper verification techniques 06-09 used (temporary debug routes rendered live, scripted end-to-end runs against real data) were possible here. This build/lint pass is the only verification this entry can claim; everything else in the spec's "Check When Done" list (real submission → real queue → real approve/reject → cross-institution isolation) is unverified beyond type-checking and code review. Whoever next has DB/Clerk access should run through the full golden path once, the same ask left open by 06/07/08/09.

- 11-funding-request-queue.md — implemented. Club-side: `app/budget/request-funding/page.tsx` mirrors 10's `/events/request-approval/page.tsx` exactly (standalone route, no chrome, `getActiveClub()` with a plain "no access" fallback, resolves the institution via `club.institutionId`, reuses `getOrSeedFormSchema(institution, "FUNDING_REQUEST")` + `DynamicForm`). `components/budget/request-funding-form.tsx` and `app/budget/request-funding/confirmation/page.tsx` mirror 10's form/confirmation pair, posting to `POST /api/budget/request-funding`.
  - `lib/form-schema-defaults.ts`: added `FUNDING_AMOUNT_FIELD_KEY = "amount"` (matches the spec's explicit instruction, same reserved-field pattern as `PROPOSED_ADMIN_EMAIL_FIELD_KEY`/`EVENT_NAME_FIELD_KEY`) and pointed `FUNDING_REQUEST_DEFAULTS`'s amount field at the constant instead of a literal string. Unlike `EVENT_NAME_FIELD_KEY`'s placeholder fallback, there's no fallback here — `FundingRequest.amount` is a dedicated required `Decimal` column (per 05-prisma.md's schema), not just a value inside `answers`/`schemaSnapshot`, so the submit route 400s outright ("Enter a valid funding amount greater than zero") if the key is missing or not a positive finite number, rather than guessing. The submitted amount is still also included in `answers` alongside the dedicated column — intentional duplication, same pattern 09 documented for `proposedAdminEmail`.
  - `POST /api/budget/request-funding` follows 10's request-approval route shape exactly (parse → JSON-parse-error handling → `getOrSeedFormSchema` → `validateFormAnswers` → pick non-FILE answers → create), creating a `FundingRequest` (`clubId`, `amount`, `answers`, `schemaSnapshot`, `status: "PENDING"`).
  - Union-side: `app/union/funding/page.tsx` replaces the 08-shell placeholder, same pattern as 09/10's queue pages — `requireOrgMode("union")` then a direct `prisma.fundingRequest.findMany({ where: { status: "PENDING", club: { institutionId } }, include: { club: true } })`. `components/union/funding-queue.tsx` (table + `Review` button, amount column formatted via a small `formatFundingAmount` helper — `Intl`-backed USD currency formatting, exported from this file since no currency-formatting utility existed anywhere else in the codebase) / `components/union/funding-review-dialog.tsx` (three-view dialog, same mount-only-when-selected pattern as 09/10 so no `useEffect` reset is needed). Per the spec's explicit "this is the one place the spec is explicit about what NOT to show" instruction: the detail view shows the requested amount prominently (large text, above the schema's `AnswerSummary`) with no itemized breakdown, and the approve view shows the amount again with no numeric input (unlike 09's baseline-budget entry field) — approving just confirms the already-fixed headline amount.
  - `app/api/union/funding/route.ts` (GET, spec-mandated API surface, not the page's actual data source — same non-consuming-its-own-API pattern already established by 09/10's GET routes). `app/api/union/funding/[requestId]/approve/route.ts` — resolves institution via `getActiveInstitution()`, 404s unless `fundingRequest.club.institutionId === institution.id` (same ownership check as 09/10), then a single `$transaction`: atomically claims the request via `updateMany({ where: { id, status: "PENDING" }, data: { status: "APPROVED" } })` (same claim-then-check pattern the "tighten atomicity" fix added to 09/10's approve routes) and, in the same transaction, `club.update({ data: { baselineBudgetAmount: { increment: fundingRequest.amount } } })` — using Prisma's `increment` rather than a read-then-write so the whole approve+budget-increase is one atomic unit; if the increment somehow fails, the status claim rolls back too, so there's no window where a request is `APPROVED` but the budget wasn't actually increased. `app/api/union/funding/[requestId]/reject/route.ts` — same ownership check, single conditional `status: "REJECTED"` update, no `Club` side effects, matching spec's "same pattern as 09/10."
  - Per the spec's explicit instruction: **flagging, not fixing**, the `Club.baselineBudgetAmount` naming question here (see Open Questions) — this spec makes the field's real meaning ("cumulative approved budget, added to over time via funding requests," not "set once at approval") unambiguous, but renaming it is out of scope for this pass.
  - Verified: `npm run build` and `npm run lint` both pass — all seven new/changed routes appear correctly as dynamic/server-rendered (`/budget/request-funding`, `/api/budget/request-funding`, `/union/funding`, `/api/union/funding`, `/api/union/funding/[requestId]/approve`, `/api/union/funding/[requestId]/reject`) alongside `/budget/request-funding/confirmation` as static; the one pre-existing `field-editor-dialog.tsx` lint error from 06 is untouched (confirmed via `git diff --stat`). This session's sandbox had no live Postgres access (`ECONNREFUSED` against `DATABASE_URL`, same gap as 10's session) and no interactive Clerk session, so the deeper live-verification techniques 06-09 used weren't possible here either — build/lint pass and code review are the only verification this entry can claim. The "approving increases the club's tracked budget by exactly the requested amount" and "cross-institution isolation holds" checks from the spec's Check-When-Done list are unverified against real data; whoever next has DB access should confirm both (the increment logic and the ownership 404 check) against a real seeded institution/club before treating this as fully done.
  - **Environment note, not a code issue**: mid-session, `npm run build` started failing with unrelated-looking errors that turned out to share one root cause — this project directory lives inside an iCloud-synced `~/Desktop` folder, and the iCloud file-provider daemon was unhealthy in this sandbox (`brctl status` returned "Access denied"). Symptoms: `package.json` and several unrelated pre-existing tracked source files (`app/api/union/applications/[applicationId]/approve/route.ts`, `app/api/union/forms/[formType]/route.ts`) intermittently read as empty/truncated (classic iCloud "dataless placeholder" behavior — `stat` reports the correct historical byte count while an actual content read returns nothing), and the repo's `test-review` branch ref (`.git/refs/heads/test-review`) was found genuinely empty/corrupted the same way. Also confirmed (again) the recurring iCloud-conflict-duplicate-file pattern first noted in 10's `route 2.ts` discovery — this session additionally found `lib/form-schema-defaults 2.ts` and `components/ui/button 2.tsx` duplicates, plus a stray `node_modules 2` directory. Fixed, in order: (1) repaired the branch ref via `git update-ref` using the exact SHA the reflog proved was last correct; (2) `rm -rf node_modules "node_modules 2" && npm install` after `node_modules` itself started throwing random unrelated-looking errors on every retry (a different package's file failing to read each time — same root cause); (3) wrote a one-off scan comparing `git hash-object <path>` (an actual content read) against each tracked file's `HEAD` blob SHA to find every file silently gone empty, rather than fixing them one at a time as the build's typechecker happened to trip over them; (4) restored the two casualty files by reading `git cat-file -p HEAD:<path>` and writing that content back through the normal edit tooling. None of this touched any file's actual intended content — every restoration was byte-for-byte identical to the committed version, confirmed via `git hash-object` matching `HEAD` afterward. Whoever hits this again: the tell is `stat`/`wc -c` reporting a plausible size while `md5`/`git hash-object` on the same file disagrees, or reads simply hang. A full `npm install` after clearing `node_modules` was what actually got a clean build; the git ref repair was a separate, unrelated casualty of the same daemon issue. The underlying iCloud-daemon health problem itself was not fixed (out of scope for a code session) — it can recur.

## Next Up (revised order — union core before Events/Brainstorm)

- New: overspend-flagging background job
- Then resume original club-workspace order: team-view, liveblocks-setup, events-board, calendar-view, budget-categories-entries, brainstorm-canvas, presence-avatars-cursors
- Then: trigger-dev-setup (reminders/digest), deployment

## Open Questions

- (from 10-event-approval-queue.md, explicitly flagged rather than solved) Event approval currently always creates a brand-new `Event` on approval. Once 14-events-board.md exists and clubs can create `IDEA`/`PLANNING` events themselves before ever requesting approval, this needs reconciling — either the request-approval submission form lets a club link an existing `Event` instead of always creating one, or the approve route needs to detect and confirm a matching existing `Event` rather than duplicate it. Whoever builds 14 should resolve this before shipping approval + board together.
- Now that Institution/Club/ClubApplication models have landed (05-prisma.md), `/select-club`'s self-serve `OrganizationList` still needs to be replaced with the real "apply to create a club" flow, and the org-redirect logic in `proxy.ts`/sidebar still needs to branch on Union Staff org vs Club org instead of treating every org the same way — 05-prisma.md only added the data layer (models + `lib/org-scope.ts`/`lib/institution-scope.ts`), it didn't touch `proxy.ts` or `/select-club`, so this is still open. Tracked here instead of as a prior open question because 03-auth.md's own scaffolding note already resolved the sequencing (self-serve org UI as a deliberate stand-in) — this is a forward pointer to the follow-up work, not a blocker that was missed.
- Real name for the project (currently "ClubOS" placeholder).
- Exact fields on the club application form — currently using reasonable defaults (name, description, category, proposed admin name/email, expected member count). Should be replaced with whatever fields the target union's actual paper form uses, once available.
- Who creates a Union Staff org for a new institution signing up — a manual/sales-assisted onboarding step, or a self-serve "register your university" flow? Not yet decided; currently assuming manual/sales-assisted since this is a B2B sale, not self-serve.
- (from 11-funding-request-queue.md, explicitly flagged rather than fixed) `Club.baselineBudgetAmount` is now a naming mismatch: it's set once at club-approval time (09) but 11's funding-request approval also adds to it over time, so it actually means "cumulative approved budget," not a one-time baseline. The spec's explicit instruction was to notice this and flag it here, not rename the field mid-pass (a rename touches 09's approve route, 11's approve route, and the `Club` model/migration). Whoever next touches budget tracking (17-budget-categories-entries or later) should consider a rename (e.g. `availableBudgetAmount`) as its own small migration.

## Resolved Decisions

- Club URLs are slug-based (`/[clubSlug]/events`, etc.) — chosen for shareable/bookmarkable links over a slug-free session-only approach.
- Platform supports many universities; each institution's union and clubs are fully isolated from every other institution.
- Club creation is never self-serve — it always goes through a `ClubApplication` reviewed by the union, approving the club and its proposed admin in a single decision.
- Event approval and funding approval are separate queues/workflows; funding is reviewed at the headline amount only, not itemized.
- Clubs have both a baseline budget (set at club approval time) and can submit one-off funding requests on top of it.
- Union-oversight features (applications, event approval, funding, overspend flags) are being built before Events board / Brainstorm canvas, since that's the part a union would actually pay for.

## Architecture Decisions

- Multi-tenant via Clerk Organizations — one org per club. No local roles table; Admin/Member maps directly to Clerk org roles.
- Liveblocks scoped to exactly two surfaces: Events board and Brainstorm canvas. No real-time elsewhere.
- Brainstorm canvas persists as a Vercel Blob snapshot (`brainstorm/{clubId}.json`); everything else lives directly in Postgres via Prisma.
- Budget tracking is full-depth: categories, per-event linking, running totals, simple report view (not a minimal expense log).
- No AI-generation features in this app — out of scope, unlike the reference project this workflow was adapted from.

## Session Notes

- Context files (`AGENTS.md`, `architecture-context.md`, `ui-context.md`, `code-standards.md`, `ai-workflow-rules.md`, `progress-tracker.md`, `project-overview.md`) are adapted from a reference project's spec-driven workflow, keeping the same design-token system, file structure conventions, and process rules — only product-specific content (canvas type, data model, features) was changed.
